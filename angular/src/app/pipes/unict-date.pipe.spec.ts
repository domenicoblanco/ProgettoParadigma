import { UnictDatePipe } from './unict-date.pipe';

describe('UnictDatePipe', () => {
  it('create an instance', () => {
    const pipe = new UnictDatePipe();
    expect(pipe).toBeTruthy();
  });
});
